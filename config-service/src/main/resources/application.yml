spring:
  application:
    name: config-service
  profiles:
    active: native
  cloud:
    config:
      server:
        bootstrap: false
        native:
          search-locations: classpath:/config-repo
        git:
          enabled: false

server:
  port: 8888

logging:
  level:
    org.springframework.cloud.config: DEBUG
